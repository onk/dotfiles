[user]
  name = onk
  email = takafumi.onaka@gmail.com

[color]
  ui = auto

[alias]
  a = add
  ap = add -p
  aw = !"git diff -w --no-color | git apply --cached"
  b = branch
  ba = branch -a
  bm = branch -a --merged
  bn = branch -a --no-merged
  bs = !"(for i in `git branch | colrm 1 2` ; do echo `git log --date=iso8601 -n 1 --pretty=\"format:[%ai] %h\" $i` $i ; done) | sort -r"
  ci = commit -v
  cam = commit -v --amend
  co = checkout
  d  = diff -b
  dc = diff --cached -b
  dw = diff --color-words --word-diff-regex='\\w+|[^[:space:]]'
  f  = fetch
  l  = log --pretty=format:\"%C(yellow)%h%Creset %C(cyan)(%an)%Creset: %s\" --date=short -30
  lg = log --graph --all --color --date=relative
  m  = merge
  mo  = merge origin/master --ff
  pr = remote prune origin
  r = rebase
  rc = rebase --continue
  ri  = rebase -i
  s = status -sb -uno

[core]
  excludesfile = ~/.gitignore
  pager = lv -c
  quotepath = false
  autocrlf = input

[format]
  pretty = %h -%d %s (%cr)

[interactive]
  singlekey = true

[push]
  default = upstream

[merge]
  ff = false

