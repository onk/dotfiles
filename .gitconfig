[user]
  name = onk
  email = takafumi.onaka@gmail.com

[color]
  ui = auto

[alias]
  a = add
  ap = add -p
  aw = !"git diff -w --no-color | git apply --cached"
  b = branch
  ba = branch -a
  bd = branch -d
  bm = branch -a --merged
  bn = branch -a --no-merged
  bs = for-each-ref --sort=-committerdate --format='%(committerdate:iso8601) %(refname:short)' refs/heads/
  ci = commit -v
  co = checkout
  d  = diff -b
  dc = diff --cached -b
  dw = diff --color-words --word-diff-regex='\\w+|[^[:space:]]'
  f  = fetch
  fp = fetch -p
  fix = commit -v --amend
  l  = log --pretty=format:\"%C(yellow)%h%Creset %C(green)%ci%Creset %C(cyan)(%an)%Creset%C(red)%d%Creset: %s\" --date=short -30
  lg = log --graph --all --color --date=relative
  m  = merge
  mo  = merge origin/master --ff
  pr = pull --prune
  ph = push origin HEAD
  phf = push origin HEAD --force-with-lease
  r = rebase
  rc = rebase --continue
  re = reset HEAD~1
  ri  = rebase -i
  s = status -sb -uno
  cancel = reset HEAD@{1}
  cancel-all = !git commit -a -m 'Temporary commit for cancel' && git reset --hard HEAD~
  sweep-branch = !git branch --delete $(git branch --merged master | egrep -v '^(\\*| ) master$')
  sweep-branch-develop = !git branch --delete $(git branch --merged develop | egrep -v '^(\\*| ) develop$')

[core]
  excludesfile = ~/.gitignore
  pager = lv -c
  quotepath = false
  autocrlf = input

[format]
  pretty = %h -%d %s (%cr)

[interactive]
  singlekey = true

[merge]
  ff = false

[diff]
  algorithm = patience

[pager]
  log = diff-highlight | less
  show = diff-highlight | less
  diff = diff-highlight | less

[ghq]
  root = /Users/onaka/src

[ghq "ssh://git.drecom.jp"]
  vcs = git

